services:
  text-to-robot:
    build: .
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - TTR_DATA_DIR=/data
    volumes:
      - robot-data:/data
    restart: unless-stopped
volumes:
  robot-data:
